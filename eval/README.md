# Recall Quality Lab assets

- `datasets/recall-core.jsonl`: versioned deterministic fixture suite.
- `reports/`: generated JSON, Markdown and comparison reports.

The Rust crate is intentionally outside the root Cargo workspace until the
integration wiring is approved. Run it with `--manifest-path` as documented in
`docs/recall-quality-lab.md`.

