{
  "name": "basemyai",
  "version": "0.1.0",
  "description": "Local memory engine for AI agents — Node.js bindings",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "napi build --platform --release --features crypto,embed --dts ../../target/basemyai-node-default.d.ts",
    "build:debug": "napi build --platform --features embed --dts ../../target/basemyai-node-debug.d.ts",
    "build:test": "napi build --platform --no-default-features --features test-util --dts ../../target/basemyai-node-test-util.d.ts",
    "typecheck": "tsc --noEmit -p tsconfig.examples.json",
    "test": "npm run typecheck && npm run build:test && jest",
    "prepublishOnly": "napi build --platform --release --features crypto,embed --dts ../../target/basemyai-node-default.d.ts"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.2",
    "@types/node": "^20",
    "jest": "^29",
    "@types/jest": "^29",
    "ts-jest": "^29",
    "typescript": "^6"
  },
  "napi": {
    "binaryName": "basemyai",
    "targets": [
      "x86_64-pc-windows-msvc",
      "x86_64-unknown-linux-gnu",
      "x86_64-apple-darwin",
      "aarch64-apple-darwin",
      "aarch64-unknown-linux-gnu"
    ]
  },
  "license": "MIT"
}
